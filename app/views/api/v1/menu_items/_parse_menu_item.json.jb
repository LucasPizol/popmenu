# frozen_string_literal: true

menu_item = menu_association.menu_item

{
  id: menu_item.id,
  name: menu_item.name,
  price: menu_association.price.to_f,
  priceFormatted: menu_association.price.format,
  createdAt: menu_item.created_at,
  updatedAt: menu_item.updated_at
}
