{
  data: render(partial: "parse_menu_item", collection: @menu_associations, as: :menu_association),
  meta: {
    currentPage: @menu_associations.current_page,
    totalPages: @menu_associations.total_pages,
    totalCount: @menu_associations.total_count
  }
}
